@using ApplicationCore.Entities;
@using Infrastructure.Data;
@using Microsoft.AspNetCore.Identity;
@inject UserManager<ApplicationUser> UserManager
@inject UserProfilesService userProfilesService
@{
    ApplicationUser appUser = await UserManager.GetUserAsync(User);
    if (appUser == null)
    {
        <script>
            window.location.href = '/';
        </script>
    }
    UserProfile profile = await userProfilesService.GetByApplicationUserIdAsync(appUser.Id.ToString());
}

<div id="layoutSidenav_nav">
    <nav class="sb-sidenav accordion sb-sidenav-light border shadow" id="sidenavAccordion">
        <div class="sb-sidenav-menu">
            <div class="nav">
                <div class="sb-sidenav-menu-heading">Hệ thống</div>
                <a class="nav-link" href="/">
                    <div class="sb-nav-link-icon"><i class="fa-solid fa-house"></i></div>
                    Trang chủ
                </a>
                <div class="sb-sidenav-menu-heading">Người dùng &amp; Vai trò</div>
                <a class="nav-link collapsed" href="#" data-bs-toggle="collapse" data-bs-target="#group1" aria-expanded="false" aria-controls="collapseLayouts">
                    <div class="sb-nav-link-icon"><i class="fa-solid fa-users"></i></div>
                    Quản lý người dùng
                    <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                </a>
                <div class="collapse" id="group1" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                    <nav class="sb-sidenav-menu-nested nav">
                        <a class="nav-link" asp-controller="UserRole" asp-action="UserProfilesEdit">Quản lý hồ sơ</a>
                        <a class="nav-link" asp-controller="UserRole" asp-action="Role">Danh sách vai trò</a>
                    </nav>
                </div>
                <a class="nav-link collapsed" href="#" data-bs-toggle="collapse" data-bs-target="#group2" aria-expanded="false" aria-controls="collapseLayouts">
                    <div class="sb-nav-link-icon"><i class="fa-solid fa-link"></i></div>
                    Thành phần chuỗi cung ứng
                    <div class="sb-sidenav-collapse-arrow"><i class="fas fa-angle-down"></i></div>
                </a>
                <div class="collapse" id="group2" aria-labelledby="headingOne" data-bs-parent="#sidenavAccordion">
                    <nav class="sb-sidenav-menu-nested nav">
                        <a class="nav-link" asp-controller="SupplyChain" asp-action="FarmingHousehold">Nông hộ</a>
                        <a class="nav-link" asp-controller="SupplyChain" asp-action="Farm">Nông trại</a>
                        <a class="nav-link" asp-controller="SupplyChain" asp-action="AgriculturalProduct">Nông sản</a>
                        <a class="nav-link" asp-controller="SupplyChain" asp-action="Transporter">Nhà vận chuyển</a>
                        <a class="nav-link" asp-controller="SupplyChain" asp-action="Retailer">Nhà bán lẻ</a>
                    </nav>
                </div>
            </div>
        </div>
        <div class="sb-sidenav-footer">
            <div class="small">Đã đăng nhập là:</div>
            @profile.FirstName @profile.LastName
        </div>
    </nav>
</div>